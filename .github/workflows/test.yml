name: Тестирование

on: [push, pull_request]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        oscript_version: ['1.8.3', 'stable']
    uses: autumn-library/workflows/.github/workflows/test.yml@main
    with:
      oscriot_version: ${{ matrix.oscript_version }}
