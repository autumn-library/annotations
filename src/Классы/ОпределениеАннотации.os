#Область ОписаниеПеременных

// Имя аннотации
Перем Имя;

// Имя типа, к которому применяется аннотация
Перем ИмяТипаАннотации;

// Параметры аннотации
Перем Параметры;

// Подчиненные аннотации
Перем Аннотации;

// КонтейнерАннотаций - Контейнер определений аннотаций, которому принадлежит данное определение.
// Используется для рекурсивной материализации вложенных аннотаций-значений.
Перем КонтейнерАннотаций;

#КонецОбласти

#Область ПрограммныйИнтерфейс

// Имя аннотации.
//
//  Возвращаемое значение:
//   Строка
//
Функция Имя() Экспорт
	Возврат Имя;
КонецФункции

// Имя типа, к которому применяется аннотация.
//
//  Возвращаемое значение:
//   Строка
//
Функция ИмяТипаАннотации() Экспорт
	Возврат ИмяТипаАннотации;
КонецФункции

// Параметры аннотации - разобранные параметры конструктора класса аннотации.
// Метод возвращает копию таблицы.
//
//  Возвращаемое значение:
//   ТаблицаЗначений - таблица параметров аннотации:
//     * Имя                     - Строка          - Имя параметра.
//     * ПоЗначению              - Булево          - Признак получения параметра по значению.
//     * ЕстьЗначениеПоУмолчанию - Булево          - Признак наличия значения по умолчанию.
//     * ЗначениеПоУмолчанию     - Произвольный    - Значение по умолчанию.
//     * Аннотации               - ТаблицаЗначений - Таблица аннотаций со следующими колонками:
//        ** Имя       - Строка          - Имя аннотации.
//        ** Параметры - ТаблицаЗначений - Таблица параметров аннотации:
//           *** Имя      - Строка - Имя параметра аннотации.
//           *** Значение - Строка - Значение параметра аннотации.
//     * Экспорт                 - Булево          - Признак экспорта параметра.
//     * Повторяемый             - Булево          - Признак повторяемого параметра.
//     * Псевдоним               - Структура       - Описание псевдонима параметра мета-аннотации.
//                                                   Неопределено, если параметр не помечен аннотацией &ПсевдонимДля:
//        ** Аннотация                     - Строка - Имя мета-аннотации, параметр которой переопределяется.
//        ** Параметр                      - Строка - Имя параметра мета-аннотации, которому присваивается значение.
//        ** ПереноситьЗначениеПоУмолчанию - Булево - Переносить ли значение по умолчанию параметра-псевдонима,
//                                                    когда значение не было передано явно.
//
Функция Параметры() Экспорт
	Возврат Параметры.Скопировать();
КонецФункции

// Подчиненные аннотации - список аннотаций над конструктором класса-аннотации.
// Метот возвращает глубокую копию таблицы.
//
//  Возвращаемое значение:
//   ТаблицаЗначений - Таблица аннотаций:
//     * Имя       - Строка          - Имя аннотации.
//     * Параметры - ТаблицаЗначений - Таблица параметров аннотации:
//       ** Имя      - Строка - Имя параметра аннотации.
//       ** Значение - Строка - Значение параметра аннотации.
//
Функция Аннотации() Экспорт
	КопияАннотации = Аннотации.Скопировать();
	Для Каждого Аннотация Из КопияАннотации Цикл
		Аннотация.Параметры = Аннотация.Параметры.Скопировать();
	КонецЦикла;
	Возврат КопияАннотации;
КонецФункции

// Выполнить проверку корректности аннотации.
//
// Выбрасывает исключение:
//  * Когда аннотация не содержит параметр, у которого нет значения по умолчанию.
//  * Когда аннотация содержит параметр, которого нет в списке параметров аннотации.
//  * Когда аннотация содержит несколько параметров без имени или с именем "Значение".
//
// Параметры:
//   Аннотация - СтрокаТаблицыЗначений - Данные аннотации:
//     * Имя       - Строка          - Имя аннотации.
//     * Параметры - ТаблицаЗначений - Таблица параметров аннотации:
//       ** Имя      - Строка - Имя параметра аннотации.
//       ** Значение - Строка - Значение параметра аннотации.
//   ПредставлениеВладельцаАннотации - Строка - Строковое представление типа или объекта
//                                              к которому применяется аннотация.
//
Процедура Проверить(Аннотация, ПредставлениеВладельцаАннотации) Экспорт

	ПривестиИменаПараметров(Аннотация);
	
	АннотацияСодержитОбязательныеПараметры(Аннотация, ПредставлениеВладельцаАннотации);
	АннотацияНеСодержитНеизвестныеПараметры(Аннотация, ПредставлениеВладельцаАннотации);
	ПовторяемыеПараметрыЗаданыКорректно(Аннотация, ПредставлениеВладельцаАннотации);
	
КонецПроцедуры

// Создать объект класса аннотации по параметрам аннотации.
//
// Параметры:
//   Аннотация - СтрокаТаблицыЗначений - Данные аннотации:
//     * Имя       - Строка          - Имя аннотации.
//     * Параметры - ТаблицаЗначений - Таблица параметров аннотации:
//       ** Имя      - Строка - Имя параметра аннотации.
//       ** Значение - Строка, Число, Булево, Дата, Неопределено, Null - литеральное значение параметра.
//                   - СтрокаТаблицыЗначений - вложенная аннотация (строка с колонками "Имя" и "Параметры",
//                                             см. РаботаСАннотациями.ЭтоАннотация). Если её определение
//                                             зарегистрировано в контейнере, рекурсивно превращается в объект
//                                             соответствующей аннотации; иначе остаётся строкой таблицы значений.
//
// Возвращаемое значение:
//   Произвольный - Объект аннотации с заданными параметрами.
//
Функция СоздатьОбъектАннотации(Аннотация) Экспорт

	ПараметрыКонструктора = Новый Массив;
	Для Каждого Параметр Из Параметры() Цикл

		Если Параметр.Повторяемый Тогда
			ЗначенияПараметра = РаботаСАннотациями.ПолучитьЗначенияПараметровАннотации(Аннотация, Параметр.Имя);
			ЗначениеПараметра = Новый Массив;
			Для Каждого Значение Из ЗначенияПараметра Цикл
				ЗначениеПараметра.Добавить(МатериализоватьЗначение(Значение));
			КонецЦикла;
		Иначе
			Значение = РаботаСАннотациями.ПолучитьЗначениеПараметраАннотации(
				Аннотация,
				Параметр.Имя,
				Параметр.ЗначениеПоУмолчанию,
				Истина
			);
			ЗначениеПараметра = МатериализоватьЗначение(Значение);
		КонецЕсли;
		ПараметрыКонструктора.Добавить(ЗначениеПараметра);
	КонецЦикла;

	ОбъектАннотации = Новый(ИмяТипаАннотации(), ПараметрыКонструктора);

	Возврат ОбъектАннотации;

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Материализовать значение параметра конструктора аннотации.
//
// Если значение является вложенной аннотацией (см. РаботаСАннотациями.ЭтоАннотация),
// а её определение зарегистрировано в контейнере данного определения, вложенная аннотация
// рекурсивно превращается в объект своей аннотации. В остальных случаях (литерал, отсутствие
// контейнера или незарегистрированная аннотация) значение возвращается как есть.
//
// Параметры:
//   Значение - Произвольный - Значение параметра (литерал или вложенная аннотация).
//
//  Возвращаемое значение:
//   Произвольный - Материализованное значение (объект вложенной аннотации либо исходное значение).
//
Функция МатериализоватьЗначение(Значение)

	Если КонтейнерАннотаций = Неопределено ИЛИ НЕ РаботаСАннотациями.ЭтоАннотация(Значение) Тогда
		Возврат Значение;
	КонецЕсли;

	ОпределениеВложеннойАннотации = КонтейнерАннотаций.ПолучитьОпределениеАннотации(Значение.Имя);
	Если ОпределениеВложеннойАннотации = Неопределено Тогда
		Возврат Значение;
	КонецЕсли;

	// Валидируем вложенную аннотацию так же, как верхнеуровневую: иначе неизвестные
	// параметры молча игнорировались бы, а обязательные неявно получали бы Неопределено.
	ПредставлениеВладельца = СтрШаблон("вложенная аннотация %1", Значение.Имя);
	ОпределениеВложеннойАннотации.Проверить(Значение, ПредставлениеВладельца);

	Возврат ОпределениеВложеннойАннотации.СоздатьОбъектАннотации(Значение);

КонецФункции

Процедура ПривестиИменаПараметров(Аннотация)

	Для Каждого ПараметрАннотации Из Аннотация.Параметры Цикл
		Если ПараметрАннотации.Имя = Неопределено Тогда
			ПараметрАннотации.Имя = "Значение";
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура АннотацияСодержитОбязательныеПараметры(Аннотация, ПредставлениеВладельцаАннотации)
	
	Для Каждого ОпределениеПараметраАннотации Из Параметры Цикл
		
		ПараметрВложеннойАннотации = Аннотация.Параметры.Найти(ОпределениеПараметраАннотации.Имя, "Имя");
		Если НЕ ОпределениеПараметраАннотации.ЕстьЗначениеПоУмолчанию
			И НЕ ОпределениеПараметраАннотации.Повторяемый
			И ПараметрВложеннойАннотации = Неопределено Тогда
			
			ВызватьИсключение СтрШаблон(
				"Аннотация %1 в классе %2 не содержит параметр %3",
				Аннотация.Имя,
				ПредставлениеВладельцаАннотации,
				ОпределениеПараметраАннотации.Имя
			);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура АннотацияНеСодержитНеизвестныеПараметры(Аннотация, ПредставлениеВладельцаАннотации)
	
	Для Каждого ПараметрАннотации Из Аннотация.Параметры Цикл
		
		ИмяПараметраВложеннойАннотации = ПараметрАннотации.Имя;
		ОпределениеПараметраАннотации = Параметры.Найти(ИмяПараметраВложеннойАннотации, "Имя");
		
		Если ОпределениеПараметраАннотации = Неопределено Тогда
			ВызватьИсключение СтрШаблон(
				"Аннотация %1 в классе %2 содержит неизвестный параметр %3",
				Аннотация.Имя,
				ПредставлениеВладельцаАннотации,
				ИмяПараметраВложеннойАннотации
			);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ПовторяемыеПараметрыЗаданыКорректно(Аннотация, ПредставлениеВладельцаАннотации)

	ИмяАннотацииПовторяемый = "Повторяемый";

	Для Каждого Параметр Из Параметры Цикл
		ЭтоПовторяемыйПараметр = РаботаСАннотациями.ПолучитьАннотацию(Параметр, ИмяАннотацииПовторяемый) <> Неопределено;
		ПараметрыАннотации = РаботаСАннотациями.ПолучитьПараметрыАннотации(Аннотация, Параметр.Имя);

		Если ПараметрыАннотации.Количество() > 1 И НЕ ЭтоПовторяемыйПараметр Тогда
			ВызватьИсключение СтрШаблон(
				"Аннотация %1 в классе %2 содержит несколько значений параметра %3, но параметр не помечен аннотацией ""%4""",
				Аннотация.Имя,
				ПредставлениеВладельцаАннотации,
				Параметр.Имя,
				ИмяАннотацииПовторяемый
			);
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

Процедура ОпределитьПовторяемостьПараметров()

	Параметры.Колонки.Добавить("Повторяемый");

	Для Каждого Параметр Из Параметры Цикл
		Параметр.Повторяемый =  НЕ РаботаСАннотациями.НайтиАннотацию(Параметр.Аннотации, "повторяемый") = Неопределено;
	КонецЦикла;

КонецПроцедуры

Процедура ОпределитьПсевдонимыПараметров()

	ИмяАннотацииПсевдоним = "ПсевдонимДля";

	Параметры.Колонки.Добавить("Псевдоним");

	Для Каждого Параметр Из Параметры Цикл

		АннотацияПсевдонима = РаботаСАннотациями.НайтиАннотацию(Параметр.Аннотации, ИмяАннотацииПсевдоним);
		Если АннотацияПсевдонима = Неопределено Тогда
			Продолжить;
		КонецЕсли;

		ИмяЦелевойАннотации = РаботаСАннотациями.ПолучитьЗначениеПараметраАннотации(АннотацияПсевдонима, "Аннотация");
		ИмяЦелевогоПараметра = РаботаСАннотациями.ПолучитьЗначениеПараметраАннотации(АннотацияПсевдонима, "Параметр");
		ПереноситьЗначениеПоУмолчанию = РаботаСАннотациями.ПолучитьЗначениеПараметраАннотации(
			АннотацияПсевдонима,
			"ПереноситьЗначениеПоУмолчанию",
			Ложь
		);

		Параметр.Псевдоним = Новый Структура(
			"Аннотация, Параметр, ПереноситьЗначениеПоУмолчанию",
			ИмяЦелевойАннотации,
			ИмяЦелевогоПараметра,
			ПереноситьЗначениеПоУмолчанию
		);

	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область Инициализация

Процедура ПриСозданииОбъекта(
	Знач _Имя,
	Знач _ИмяТипаАннотации,
	Знач _Параметры,
	Знач _Аннотации,
	Знач _КонтейнерАннотаций = Неопределено
)

	Имя = _Имя;
	ИмяТипаАннотации = _ИмяТипаАннотации;
	Аннотации = _Аннотации;
	Параметры = _Параметры;
	КонтейнерАннотаций = _КонтейнерАннотаций;

	ОпределитьПовторяемостьПараметров();
	ОпределитьПсевдонимыПараметров();

КонецПроцедуры

#КонецОбласти